'use strict';

const request = require('supertest');
const expect = require('chai').expect;

const app = 'http://localhost:3000';

describe('GET /todos', function() {
  xit('respond with all todos', function() {
    // put your impelementation here
  });
});
